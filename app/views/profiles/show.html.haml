.profile
  %h1= profile.full_name
  %h2= profile.tagline
  %dl
    %dt Github:
    %dd= profile.nickname
    %dt Current Position:
    %dd= profile.position
    %dt Email:
    %dd= profile.email

  %section.projects
    %h3 Projects
    - current_user.whitelist.each do |repo|
      .project
        %h4= repo.name
        %p= repo.description
        %a{href: repo.url} code
    .clearfix

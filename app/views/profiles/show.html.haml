.profile
  %h1= "#{profile.full_name}'s Profile"
  %h2= profile.tagline
  %dl
    %dt Github:
    %dd= profile.nickname
    %dt Current Position:
    %dd= profile.position
    %dt Email:
    %dd= profile.email

  %section.projects
    %h3 Projects
    - current_user.whitelist.each do |repo|
      .project
        %a{href: repo.url}= repo.name
        %p= repo.description
    .clearfix

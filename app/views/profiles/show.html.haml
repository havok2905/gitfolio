.profile
  %h1= "#{profile.full_name}'s Profile"
  %dl
    %dt Github:
    %dd= profile.nickname
    %dt Current Position:
    %dd= profile.position
    %dt Email:
    %dd= profile.email
  %h2= profile.tagline



  %ul
    - profile.repos.each do |repo|
      %li
        %a{href: repo.url}= repo.name
      %li= repo.description
